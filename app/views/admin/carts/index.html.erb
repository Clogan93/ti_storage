<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>Created</th>
      <th>Transaction</th>
    </tr>
  </thead>
  <tbody>
    <% @carts.each do |cart| %>
    <tr>
      <td><%= link_to(cart.id, [:admin, cart]) %></td>
      <td><%= cart.created_at %></td>
      <td><%= cart.payment_data.transaction_id rescue "n/a" %></td>
    </tr>
    <% end %>
  </tbody>
</table>
